""" Customer Relationship Management (CRM) module

Data table structure:
    - id (string)
    - name (string)
    - email (string)
    - subscribed (int): Is subscribed to the newsletter? 1: yes, 0: no
"""


from model import data_manager, util


def new_customer():
    name = view.get_input('Enter the name')
    email = view.get_input('Enter the email address')
    while subscribed != 1 or 0:
        subscribed = int(view.get_input('Is subscribed to the newsletter? 1: yes, 0: no')
    util.generate_id((number_of_small_letters=4,
                number_of_capital_letters=2,
                number_of_digits=2,
                number_of_special_chars=2,
                allowed_special_chars=r"_+-!")
    # write data in crm.csv
        

    




DATAFILE = "model/crm/crm.csv"
HEADERS = ["id", "name", "email", "subscribed"]
